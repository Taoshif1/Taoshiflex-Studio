"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
export default function ClientAuthCallback(){const router=useRouter(),[message,setMessage]=useState("Securing your session…");useEffect(()=>{async function complete(){const hash=new URLSearchParams(location.hash.slice(1)),accessToken=hash.get("access_token"),refreshToken=hash.get("refresh_token"),expiresIn=hash.get("expires_in");await Promise.resolve();if(!accessToken){setMessage("This sign-in link is invalid or expired.");return}try{const response=await fetch("/api/client/auth",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"session",accessToken,refreshToken,expiresIn})});if(!response.ok)throw new Error((await response.json().catch(()=>({}))).error||"Sign-in failed.");history.replaceState(null,"","/client/auth/callback");router.replace("/client");router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Sign-in failed.")}}void complete()},[router]);return <main className="client-shell client-login"><section className="client-login-panel"><p className="eyebrow">Private / Secure access</p><h1>{message}</h1><p>You will be returned to your authorized project workspace.</p></section></main>}

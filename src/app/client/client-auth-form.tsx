"use client";
import { FormEvent, useRef, useState } from "react";
import { useRouter } from "next/navigation";

async function authRequest(body:unknown){const response=await fetch("/api/client/auth",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),result=await response.json().catch(()=>({})) as {error?:string};if(!response.ok)throw new Error(result.error||"Sign-in could not be completed.")}
export function ClientAuthForm(){const router=useRouter(),pendingRef=useRef(false),[email,setEmail]=useState(""),[sent,setSent]=useState(false),[pending,setPending]=useState(false),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();if(pendingRef.current)return;pendingRef.current=true;setPending(true);setMessage("");const form=new FormData(event.currentTarget);try{if(!sent){await authRequest({action:"request",email});setSent(true);setMessage("Check your email for a secure sign-in link or one-time code.")}else{await authRequest({action:"verify",email,token:String(form.get("token")??"").trim()});router.refresh()}}catch(error){setMessage(error instanceof Error?error.message:"Sign-in could not be completed.")}finally{pendingRef.current=false;setPending(false)}}return <form className="client-auth-form" onSubmit={submit}><label>Email address<input type="email" value={email} onChange={event=>setEmail(event.target.value)} autoComplete="email" disabled={sent||pending} required/></label>{sent?<label>One-time code<input name="token" inputMode="numeric" autoComplete="one-time-code" minLength={6} maxLength={12} required/></label>:null}<p className="client-form-note" aria-live="polite">{message||"Access is available only to email addresses assigned to a Client Project."}</p><button className="action action-solid" disabled={pending}>{pending?"Please wait…":sent?"Verify and continue":"Email secure access"}</button>{sent?<button className="client-text-button" type="button" disabled={pending} onClick={()=>{setSent(false);setMessage("")}}>Use another email</button>:null}</form>}
export function ClientLogout(){async function logout(){await fetch("/api/client/auth",{method:"DELETE"});location.href="/client"}return <button className="client-logout" onClick={logout}>Sign out</button>}

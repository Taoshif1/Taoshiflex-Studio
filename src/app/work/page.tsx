import type { Metadata } from "next";
import Link from "next/link";
import { getPublishedProjects } from "@/lib/studio-data";
import { ResponsiveMedia } from "@/components/ui/primitives";
import "./work.css";
export const metadata:Metadata={title:"Selected Work",description:"Selected commerce, business system and digital product work by Taoshiflex Studio.",alternates:{canonical:"/work"}};
export const dynamic="force-dynamic";
export default async function WorkPage(){const projects=await getPublishedProjects();return <div className="work-index container"><header><p className="eyebrow">Selected work / Curated</p><h1 className="display">Business problems,<br/>made tangible.</h1><p>A focused selection of commercial and internal product work. Outcomes are described honestly; technology stays in service of the problem.</p></header><div className={`work-list ${projects.length?"":"empty"}`}>{projects.length?projects.map((project,index)=><article key={project.slug} style={{"--accent":project.accent} as React.CSSProperties}><div className="work-number technical">{String(index+1).padStart(2,"0")}</div><Link href={`/work/${project.slug}`}><ResponsiveMedia accent={project.accent} label={project.name} media={project.coverMedia}/></Link><div><p className="technical">{project.category} / {project.status}</p><h2><Link href={`/work/${project.slug}`}>{project.name}</Link></h2><p>{project.summary}</p><Link className="action" href={`/work/${project.slug}`}>Read case study <span aria-hidden>↗</span></Link></div></article>):<div><p className="eyebrow">Editorial pause</p><h2>No project is currently published.</h2><p>The public selection is intentionally empty while the Studio curates verified case studies.</p><Link className="action" href="/start-a-project">Discuss relevant work →</Link></div>}</div></div>}
